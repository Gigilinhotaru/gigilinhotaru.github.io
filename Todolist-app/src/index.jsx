import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App.jsx'; // Corrected import for default export

ReactDOM.createRoot( 
  document.querySelector('#root')
).render(<App />)